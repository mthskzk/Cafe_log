<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>フォロー中 <%= @customer.followings.count %>人</h2>
      <% if @followings.exists? %>
        <%= render 'index', customers: @followings %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>